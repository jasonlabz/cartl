-- ============================================================
-- 种子数据示例
-- ============================================================
-- 约定：
--   1. 所有 INSERT 必须使用 ON CONFLICT ... DO NOTHING 保证幂等
--   2. 每次启动都在 DDL 迁移完成后执行
--   3. 执行失败不会阻塞启动（仅告警日志）
--   4. 按文件名排序依次执行
-- ============================================================

-- 示例：默认角色
-- INSERT INTO roles (code, name, description) VALUES
--     ('R_SUPER', '超级管理员', '拥有所有权限'),
--     ('R_ADMIN', '管理员', '拥有管理权限'),
--     ('R_USER', '普通用户', '基本操作权限')
-- ON CONFLICT (code) DO NOTHING;
